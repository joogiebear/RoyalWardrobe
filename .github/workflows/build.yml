name: Build

# Compile on every push/PR to main. Catches breakage (e.g. an unresolved
# dependency) long before the Friday release job runs.
on:
  push:
    branches: [ main ]
    paths-ignore:
      - '**.md'
      - '.github/release/**'
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # JDK 25: this plugin compiles against paper-api 26.2, whose artifact is Java 25
      # bytecode — a Java 21 javac can't read it.
      - name: Set up JDK 25
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '25'
          cache: maven

      - name: Build
        run: mvn -B package

      - name: Upload jar
        uses: actions/upload-artifact@v4
        with:
          name: ${{ github.event.repository.name }}
          path: target/${{ github.event.repository.name }}.jar
          if-no-files-found: error
